check_interval: 30  # seconds
http_client:
  timeout: 30  # seconds
kafka:
  bootstrap_servers: $KAFKA_SVC_URI  # bootstrap_servers: KAFKA_SVC_URI
  topic: 'demo'
  test_topic: 'test'
postgres:
    dbname: $PGDATABASE 
    dbpass: $PGPASS
    dbuser: $PGUSER 
    host: $PGHOST 
    port: $PGPORT 
